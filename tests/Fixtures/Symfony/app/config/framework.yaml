framework:
    secret: ThisIsVeryNotSecret!
    default_locale: en
    php_errors:
        log: true
    test: ~
    session:
        storage_factory_id: session.storage.factory.native
        enabled: false
